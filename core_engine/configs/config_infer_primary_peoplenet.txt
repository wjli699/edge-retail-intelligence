[property]
gpu-id=0

# Input normalisation: [0,1] range → scale = 1/255
net-scale-factor=0.0039215697906911373
model-color-format=0

# Model files — paths relative to THIS config file's directory (configs/).
# ONNX lives at repo root models/peoplenet/ (see scripts/download_peoplenet.sh).
onnx-file=../../models/peoplenet/resnet34_peoplenet_int8.onnx
# Engine filename must match what TRT auto-generates: <onnx>_b<batch>_gpu<id>_<precision>.engine
# Without an INT8 calibration table, TRT falls back to FP16 and uses this exact name.
model-engine-file=../../models/peoplenet/resnet34_peoplenet_int8.onnx_b1_gpu0_fp16.engine
labelfile-path=../configs/peoplenet_labels.txt

# network-mode: 0=FP32, 1=INT8, 2=FP16
# FP16 — no INT8 calibration table available for this model download.
# Tactic-memory warnings during the one-time build are normal; TRT skips
# kernels that don't fit alongside the live RTSP decoder and uses alternatives.
network-mode=2

batch-size=1
num-detected-classes=3
# Only track persons (class 0). Bag (1) and face (2) are filtered out before
# they reach the tracker, so they never get tracking IDs or OSD bboxes.
filter-out-class-ids=1;2
interval=0

# nvinfer identity used when multiple GIEs are chained
gie-unique-id=1

# process-mode: 1=primary detector
process-mode=1

# network-type: 0=detector
network-type=0

# cluster-mode: 2=NMS (suitable for detection heads)
cluster-mode=2

# PeopleNet ResNet-34 input: C×H×W = 3×544×960
infer-dims=3;544;960

# TensorRT explicit-batch engines expose ONNX tensor names with a :0 suffix.
output-blob-names=output_bbox/BiasAdd:0;output_cov/Sigmoid:0

[class-attrs-all]
pre-cluster-threshold=0.2
topk=20
nms-iou-threshold=0.5

# Person — higher threshold to reduce false positives
[class-attrs-0]
pre-cluster-threshold=0.3
topk=20

# Bag
[class-attrs-1]
pre-cluster-threshold=0.2

# Face
[class-attrs-2]
pre-cluster-threshold=0.2
