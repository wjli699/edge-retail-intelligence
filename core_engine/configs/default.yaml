version: 1

sources:
  - uri: "rtsp://192.168.9.146:18554/stream3"
    enabled: true

models:
  # Path relative to the working directory where the binary is invoked.
  # Run from core_engine/build/ so "../configs/..." resolves correctly for
  # nvinfer (which resolves onnx-file / model-engine-file relative to its own
  # config file location).
  detector: "configs/config_infer_primary_peoplenet.txt"
  tracker:  "configs/config_tracker_NvDCF.yml"

output:
  mode: stdout
  # mode: file
  # file: /tmp/edge_retail_events.jsonl
